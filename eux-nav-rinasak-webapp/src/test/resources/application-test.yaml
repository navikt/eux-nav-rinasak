spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:${mock-oauth2-server.port}/issuer1
          audiences:
            - demoapplication

  flyway:
    clean-disabled: false
